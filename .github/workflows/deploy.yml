name: Auto Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code & restart docker
        run: |
          cd ~/cicd-demo
          git pull origin main
          docker compose down
          docker compose up -d --build
