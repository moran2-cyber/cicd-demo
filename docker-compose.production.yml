services:

  nginx:
    ports:
      - "8080:80"
    restart: always

  api:
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M

  postgres:
    restart: always

  redis:
    restart: always
