services:

  nginx:
    ports:
      - "80:80"

  api:
    build: .
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
    ports:
      - "3000:3000"

  postgres:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"

  grafana:
    # ports:
    #  - "3001:3000
